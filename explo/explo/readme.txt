# Open the folder and run : python install_from_readme.py

# Install numpy
pip install numpy

# Install OpenCV with contrib modules for SIFT support
pip install opencv-contrib-python

# Install scikit-image for image labeling and regionprops
pip install scikit-image